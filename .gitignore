.env
.env.*
# Prod state DB holds prod NetSuite internal IDs — never commit it.
# (The sandbox state/load_state.db is intentionally tracked.)
state/load_state_prod.db

.venv

__pycache__


data/*
.DS_Store

logs/*

scripts/reports/*

# place to store temporary files for testing and experimentation
playground/*
